pipeline {
    agent any
    stages {
        
        stage('Run Tests') {
            steps {
                script {
                    // Run your tests here and capture the output
                    sh 'python3 TestLoginAPI.py'
                   
                }
            }
        }
    }
   
}
